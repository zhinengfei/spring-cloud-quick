# Feign去消费服务
# Feign 是一个声明式的伪Http客户端 ，它使得http客户端变得更简单。使用Feign,
# 只需要创建一个接口并注解。它具有可拔的注解特性，使用Feign注解和JAR-RS注解。
# Feign支持可拔的编码器和解码器 。
# Feign 默认集成了ribbon ,并和Eureka结合 ，默认实现了负载均衡的结果
# Feign整合了Hytrix 具有熔断的能力